import { View, Text } from 'react-native'
import React from 'react'
import CustomHeader from '../../shared/Components/CustomHeader'

export default function Event() {
    return (
        <>
        <CustomHeader headename='Event' icon={true} />
        </>
    )
}